---
title: 射速机制
tag: [猎场, 机制]
keywords:
  - 射速
  - 射速加成
  - 基础射速
  - 射击间隔
  - 急速狂热
  - 幸运龙炎
  - 不死狂热
page-width: 3xl
---

<Callout color="blue">本文的“射速”只讨论玩家枪械的射击速度。召唤物攻击频率、近战攻速和固定动作频率不属于本文的射速加成。</Callout>

## 射速加成

射速加成是在**当前基础射速**上进行的倍率提升。通常情况下，射速加成上限为 **50%**：

```text
最终 RPM = 当前基础 RPM × (1 + 射速加成)
最终射击间隔 = 当前基础射击间隔 ÷ (1 + 射速加成)
```

因此，`+50%` 射速表示 RPM 变为当前基础值的 **1.5 倍**，射击间隔缩短约 **33.33%**，不是射击间隔减半。

<Callout color="yellow">下方列出的是每个来源自身的实际变化值，不代表不同来源一定能够互相叠加；达到通常的 50% 上限后，也不能继续按百分比直接相加。</Callout>

### 武器技能

<FireRateCatalog group="weapons" />

### 插件

<FireRateCatalog group="perks" />

### 超限卡片

超限卡片按猎场正式卡片表独立收录。同名插件与超限卡片是两个索引入口，因此可能同时出现在两个分组中。

<FireRateCatalog group="overlimit" />

## 基础射速变化

基础射速与射速加成不是一回事。星海狂想、纯白至上和冥河之矛会在持续射击过程中改变自身的基础射击间隔，这部分变化不占用通常的 `+50%` 射速加成上限。

<FireRateCatalog group="base" />

星海狂想的主动技能仍然是 **+50% 射速加成**，不是把基础射速直接改为 3.3 倍。只有在涡轮增压已经达到约 **2.2 倍**基础射速时，再乘以主动技能的 `1.5`，组合结果才约为初始射速的 **3.3 倍**。
